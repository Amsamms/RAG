# OpenAI API Configuration - USER INPUT ONLY
# For security reasons, API keys should ONLY be entered through the application interface
# Do NOT store API keys in environment files

# Optional: Use different embedding model  
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Default OpenAI model (can be overridden by user)
DEFAULT_OPENAI_MODEL=gpt-3.5-turbo